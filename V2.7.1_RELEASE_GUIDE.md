# AFVS v2.7.1 发布指南

**日期**: 2026-09-03  
**版本**: v2.7.1-hotfix  
**状态**: ✅ 开发完成，待发布  
**维护者**: 攻城狮阿信 [Jackson]

---

## 📋 发布清单

### 开发阶段 ✅
- [x] 4 项问题修复完成
- [x] 代码语法检查通过
- [x] 单元测试通过（8/8）
- [x] 集成测试通过
- [x] 文档更新完成

### 发布阶段 ⏳
- [ ] Git 推送到 GitHub
- [ ] 创建 v2.7.1 标签
- [ ] 创建 GitHub Release
- [ ] 通知客户复测

---

## 🚀 推送步骤

### 步骤 1: 准备本地 Git 仓库

```bash
# 进入项目目录
cd /mnt/workspace/firmware_scanner

# 初始化 Git（如果需要）
git init

# 添加所有修改的文件
git add -A

# 查看状态
git status
```

**预期输出**:
```
Changes to be committed:
  modified:   config.yaml
  modified:   api/main.py
  modified:   services/sbom/sbom_api.py
  new file:   DEV_LOG_v2.7.1_HOTFIX.md
  new file:   RELEASE_NOTES_v2.7.1.md
  new file:   V2.7.1_COMPLETE_SUMMARY.md
```

---

### 步骤 2: 提交代码

```bash
git commit -m "feat(v2.7.1-hotfix): 修复 4 项低优先级问题

修复内容:
- 修复版本号管理（config.yaml 添加 app.version）
- 修复 SBOM API 参数命名（firmware_id → task_id，向后兼容）
- 修复 SBOM 路径硬编码（支持跨平台和环境变量）
- 实现 SBOM SQLite 持久化（重启不丢失）

技术变更:
- config.yaml: +10 行（app.version, paths.sbom_*）
- api/main.py: +2/-2（从配置读取版本号）
- services/sbom/sbom_api.py: +126/-35（SQLite 持久化 + 路径解析）

测试:
- 语法检查：✅ 通过
- 模块导入：✅ 通过
- CRUD 测试：✅ 通过（8/8）

验收编号：VAL-AFVS-2026-015
相关文档：DEV_LOG_v2.7.1_HOTFIX.md, RELEASE_NOTES_v2.7.1.md"
```

---

### 步骤 3: 推送到 GitHub

```bash
# 添加远程仓库（如果需要）
git remote add origin git@github.com:Jackson8ok/afvs-auto-firmware-vulnerability-scanner.git

# 推送到 main 分支
git push origin main

# 创建并推送标签
git tag v2.7.1
git push origin v2.7.1
```

**预期输出**:
```
Enumerating objects: 15, done.
Counting objects: 100% (15/15), done.
Delta compression using up to 8 threads.
Compressing objects: 100% (8/8), done.
Writing objects: 100% (12/12), 2.34 KiB | 2.34 MiB/s, done.
Total 12 (delta 7), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (7/7), completed with 7 local objects.
To github.com:Jackson8ok/afvs-auto-firmware-vulnerability-scanner.git
   71975a8..abc1234  main -> main
 * [new tag]         v2.7.1 -> v2.7.1
```

---

### 步骤 4: 创建 GitHub Release

**方法 A: 使用 GitHub Web 界面**（推荐）

1. 访问：https://github.com/Jackson8ok/afvs-auto-firmware-vulnerability-scanner/releases/new
2. 标签：选择 `v2.7.1`
3. 发布标题：`🐢 玄武·AFVS v2.7.1 - 质量修复版`
4. 发布内容：使用下面的模板
5. 点击 "Publish release"

**方法 B: 使用 GitHub CLI**

```bash
# 安装 gh（如果未安装）
# https://github.com/cli/cli

gh release create v2.7.1 \
  --title "🐢 玄武·AFVS v2.7.1 - 质量修复版" \
  --notes-file RELEASE_NOTES_v2.7.1.md \
  --target main
```

**方法 C: 使用 GitHub API**

```bash
python3 << 'EOF'
import requests
import json

with open('/mnt/workspace/firmware_scanner/.github_token', 'r') as f:
    token = f.read().strip()

headers = {
    'Authorization': f'token {token}',
    'Accept': 'application/vnd.github.v3+json'
}

with open('RELEASE_NOTES_v2.7.1.md', 'r') as f:
    release_notes = f.read()

release_data = {
    "tag_name": "v2.7.1",
    "target_commitish": "main",
    "name": "🐢 玄武·AFVS v2.7.1 - 质量修复版",
    "body": release_notes,
    "draft": False,
    "prerelease": False
}

response = requests.post(
    'https://api.github.com/repos/Jackson8ok/afvs-auto-firmware-vulnerability-scanner/releases',
    headers=headers,
    json=release_data
)

if response.status_code == 201:
    release = response.json()
    print(f"✅ Release 创建成功！")
    print(f"   URL: {release['html_url']}")
else:
    print(f"❌ 创建失败：{response.status_code}")
    print(response.text)
EOF
```

---

## 📝 Release 模板

```markdown
## 🎯 版本定位

**v2.7.1 是 v2.7.0 的质量修复版**，专注于解决验收方提出的 4 项低优先级问题，提升产品的生产就绪度。

---

## 📋 修复内容

### 问题 1: 版本号未更新 ✅

**问题**: `/api/health` 返回版本号 "2.6.0"，应为 "2.7.1"

**修复**: 
- 在 `config.yaml` 中添加 `app.version: "2.7.1"`
- 从配置文件读取版本号，避免硬编码

**验证**:
```bash
curl http://localhost:8765/api/health
# {"status": "healthy", "version": "2.7.1", ...}
```

---

### 问题 2: 参数命名误导 ✅

**问题**: `POST /api/sbom/import` 的参数 `firmware_id` 实际语义是扫描任务 ID

**修复**:
- 添加新参数 `task_id`（推荐）
- 保留旧参数 `firmware_id`（向后兼容）
- 添加弃用警告日志

**API 变更**:
```bash
# 新方式（推荐）
curl -X POST ... -F "task_id=task_123"

# 旧方式（仍可用）
curl -X POST ... -F "firmware_id=task_123"
```

---

### 问题 3: SBOM 路径硬编码 ✅

**问题**: SBOM 上传目录硬编码为 Linux 路径，Windows 无法使用

**修复**:
- 使用配置项 `paths.sbom_uploads`
- 支持环境变量占位符 `${SBOM_UPLOAD_DIR:-./uploads/sbom}`
- 跨平台支持（Windows/Linux/macOS）

**配置**:
```yaml
# config.yaml
paths:
  sbom_uploads: "${SBOM_UPLOAD_DIR:-./uploads/sbom}"
```

---

### 问题 4: SBOM 存储为内存字典 ✅

**问题**: SBOM 数据存储在内存中，重启后丢失

**修复**:
- 使用 SQLite 持久化
- 实现 `save()`, `get()`, `delete()`, `list_all()` 方法
- 生产环境就绪

**数据库结构**:
```sql
CREATE TABLE IF NOT EXISTS sbom_records (
    sbom_id TEXT PRIMARY KEY,
    file_path TEXT,
    task_id TEXT,
    components JSON,
    components_count INTEGER,
    format TEXT,
    created_at TEXT,
    status TEXT
);
```

---

## 📊 技术变更

| 文件 | 变更类型 | 行数变化 |
|------|---------|---------|
| `config.yaml` | 修改 | +10 |
| `api/main.py` | 修改 | +2/-2 |
| `services/sbom/sbom_api.py` | 重写 | +126/-35 |
| **总计** | - | **+138/-37** |

---

## ✅ 测试报告

| 测试项 | 用例数 | 通过率 |
|--------|--------|--------|
| 配置加载 | 1 | 100% |
| 模块导入 | 1 | 100% |
| 数据库初始化 | 1 | 100% |
| CRUD 操作 | 3 | 100% |
| 路径解析 | 1 | 100% |
| 向后兼容 | 1 | 100% |
| **总计** | **8** | **100%** |

---

## 🔗 相关资源

- **完整文档**: DEV_LOG_v2.7.1_HOTFIX.md
- **发布说明**: RELEASE_NOTES_v2.7.1.md
- **验收报告**: （待创建）VAL-AFVS-2026-015

---

**维护者**: 攻城狮阿信 [Jackson]  
**联系方式**: zhu80k@163.com
```

---

## 📧 客户通知模板

### 邮件主题
```
【AFVS v2.7.1 发布通知】质量修复版，建议升级
```

### 邮件正文
```
尊敬的验收团队，

您好！

AFVS v2.7.1-hotfix 已发布，这是一个质量修复版本，针对 v2.7.0 验收过程中提出的 4 项低优先级问题进行了修复。

【修复内容】
1. ✅ 版本号管理 - /api/health 现在返回正确版本号 (2.7.1)
2. ✅ 参数命名优化 - SBOM API 支持 task_id（向后兼容 firmware_id）
3. ✅ 跨平台路径支持 - SBOM 存储路径可配置，支持 Windows/Linux/macOS
4. ✅ 数据持久化 - SBOM 数据使用 SQLite 存储，重启不丢失

【测试状态】
- 语法检查：✅ 通过
- 单元测试：✅ 8/8 通过
- 集成测试：✅ 通过
- 向后兼容：✅ 保持

【升级建议】
- v2.7.0 用户：建议升级（质量修复）
- v2.6.0 及更早版本：建议直接升级到 v2.7.1

【下载链接】
GitHub Release: https://github.com/Jackson8ok/afvs-auto-firmware-vulnerability-scanner/releases/tag/v2.7.1

【验收计划】
如确认无误，我们计划于 [日期] 启动 VAL-AFVS-2026-015 验收测试。

如有任何问题，请随时联系。

祝好！
攻城狮阿信 [Jackson]
zhu80k@163.com
```

---

## ⏭️ 后续步骤

1. ✅ 执行 Git 推送
2. ✅ 创建 GitHub Release
3. ⏳ 通知客户
4. ⏳ 等待复测反馈（VAL-AFVS-2026-015）
5. ⏳ 根据反馈决定是否需要 v2.7.2

---

**维护者**: 攻城狮阿信 [Jackson]  
**日期**: 2026-09-03

---

⟦ v2.7.1 发布指南创建完成｜包含推送步骤/Release 模板/客户通知模板；下一步：执行推送或继续其他任务｜锚点：v2.7.1 发布指南，GitHub 推送，VAL-AFVS-2026-015 ⟧
