# AFVS v2.7.2-hotfix 修复总结

**日期**: 2026-09-03  
**验收编号**: VAL-FWSCAN-2026-016  
**状态**: ✅ **已修复并推送**

---

## 📊 验收报告关键发现

### ⭕ 有条件通过

**上轮缺陷修复情况**: 6/6 全部响应

| # | 上轮缺陷 | 本版状态 | 实测 |
|:-:|---------|---------|:--:|
| 1 | Phase 4 融合未接入 API | ✅ 已接入 | `/api/scan` 支持 `sbom_id` |
| 2 | 版本号管理混乱 | ✅ 已改配置驱动 | ⚠️ 但 config 值为 "2.7.1"，与包名 2.7.2 差一号 |
| 3 | `firmware_id` 参数命名误导 | ✅ 已修复 | 参数说明已更正 |
| 4 | SBOM 路径硬编码 | ✅ 已修复 | `/mnt/workspace` 已消除 |
| 5 | SBOM 内存存储 | ✅ 已持久化 | sqlite 存储 |
| 6 | 无变更说明文档 | ✅ 已补齐 | Release Notes + Phase 4 定位说明 |

---

### ❌ 新发现 P0 缺陷：Phase 4 融合运行时崩溃

**实测证据**:
```json
{
  "enabled": true,
  "sbom_id": "sbom_324aaa1c8824",
  "error": "the JSON object must be str, bytes or bytearray, not list",
  "fallback": "standard_scan"
}
```

**根因**: `scanner/task_queue.py` 第 659 行双重 JSON 反序列化

**修复前**:
```python
# sbom_db.get() 返回时已反序列化 components 为 list
for comp in json.loads(sbom_record['components']):  # ❌ 对 list 再次 json.loads → 崩溃
```

**修复后**:
```python
# 单行修复：正确处理类型
comps_raw = sbom_record['components']
sbom_components_list = comps_raw if isinstance(comps_raw, list) else json.loads(comps_raw)  # ✅
```

---

## 🔧 修复清单

### 代码修复

| 文件 | 变更 | 说明 |
|------|------|------|
| `scanner/task_queue.py` | +3 行 | 修复双重 JSON 反序列化（第 659 行） |
| `config.yaml` | +1/-1 行 | 版本号 2.7.1 → 2.7.2 |
| `tests/test_fusion_api.py` | +260 行 | 新增融合端到端集成测试（验收门禁） |

### 测试验证

| 测试项 | 状态 | 详情 |
|--------|------|------|
| 语法检查 | ✅ 通过 | 所有 Python 文件 |
| 模块导入 | ✅ 通过 | SBOM API + 融合引擎 |
| 融合端到端测试 | ✅ 通过 | 5/5 测试用例 |
| - SBOM 序列化/反序列化 | ✅ 通过 | 无双重反序列化 |
| - 融合引擎 A/B/C 分级 | ✅ 通过 | A:2, B:1, C:1 |
| - 加权 CVE 计算 | ✅ 通过 | 权重准确 |

---

## 📦 交付信息

### Git 提交

```
40ecc64 fix(v2.7.2-hotfix): 修复 Phase 4 融合运行时崩溃（VAL-FWSCAN-2026-016）
```

### GitHub Release

- **Tag**: v2.7.2-hotfix
- **URL**: https://github.com/Jackson8ok/afvs-auto-firmware-vulnerability-scanner/releases/tag/v2.7.2-hotfix
- **Latest Release**: v2.7.2（保持不变）

### 交付包（可选）

如需构建交付包：
```bash
cd /mnt/workspace/firmware_scanner
./scripts/package_release_v2.7.2-hotfix.sh
```

---

## ✅ 验收标准对比

| 标准 | 要求 | v2.7.2-hotfix 状态 |
|------|------|-------------------|
| Phase 4 API 可用 | `/api/scan` 支持 `sbom_id` | ✅ 已实现 |
| 融合结果返回 | A/B/C 分层 + 加权统计 | ✅ 已实现 |
| 无运行时崩溃 | 融合路径不崩溃 | ✅ 已修复 |
| 集成测试覆盖 | 融合端到端测试 | ✅ 已补充 |
| 版本号一致 | config.yaml = 交付包名 | ✅ 已修复 |

---

## 📧 客户沟通建议

### 立即通知

```
主题：【AFVS v2.7.2-hotfix 发布通知】Phase 4 融合 P0 缺陷已修复

尊敬的验收团队，

感谢您对 VAL-FWSCAN-2026-016 验收报告的详细反馈。

我们已识别并修复了 Phase 4 融合运行时崩溃的 P0 缺陷：

🔴 问题根因：scanner/task_queue.py 第 659 行双重 JSON 反序列化
✅ 修复方案：单行类型检查修复
✅ 测试验证：融合端到端集成测试 5/5 通过
✅ 交付状态：GitHub Release v2.7.2-hotfix 已发布

复测建议：
1. 导入 SBOM
2. 带 sbom_id 扫描
3. 验证 phase4_fusion 字段包含 A/B/C 分层和加权统计

预计复测工作量：< 1 小时

给您带来的不便，我们深表歉意。

攻城狮阿信 [Jackson]
2026-09-03
```

---

## 🎯 经验教训

### 流程改进

1. **集成测试门禁**
   - 问题：紧急修复计划承诺的测试脚本未交付
   - 改进：建立交付检查清单，包含测试脚本验证

2. **版本号管理**
   - 问题**: config.yaml 版本号与交付包名不一致
   - 改进**: CI/CD 自动注入版本号

3. **端到端测试**
   - 问题：融合功能单元测试通过，但端到端测试缺失
   - 改进：关键路径必须覆盖端到端测试

### 技术改进

1. **JSON 序列化/反序列化**
   - 问题：存储层已反序列化，应用层再次反序列化
   - 改进：统一序列化策略，增加类型检查

2. **错误处理**
   - 问题：融合崩溃后静默回退，用户不知情
   - 改进：增加详细错误日志和告警

---

## 📋 复测检查清单

验收方复测时建议验证以下项目：

- [ ] 导入 SBOM（任意格式）
- [ ] 扫描时传入 `sbom_id` 参数
- [ ] 检查返回结果的 `phase4_fusion` 字段
- [ ] 验证 `evidence_summary` 包含 A/B/C 分层
- [ ] 验证 `weighted_cve` 包含加权统计
- [ ] 确认无 `error` 字段（融合路径未崩溃）

---

**维护者**: 攻城狮阿信 [Jackson]  
**邮箱**: zhu80k@163.com  
**日期**: 2026-09-03

---

⟦ v2.7.2-hotfix 修复总结完成｜状态：P0 缺陷修复✅ + 测试补充✅ + Release 发布✅；下一步：客户通知 + 复测支持｜锚点：v2.7.2-hotfix, VAL-FWSCAN-2026-016, Phase 4 融合修复 ⟧
