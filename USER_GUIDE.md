# 玄武固件扫描平台 - 用户指南

**版本**: v2.5.0  
**最后更新**: 2026-08-19  
**维护者**: 攻城狮阿信 [Jackson]  
**联系**: zhu80k@163.com

---

## 📖 目录

1. [快速开始](#快速开始)
2. [能力边界](#能力边界)
3. [扫描目标类型](#扫描目标类型)
4. [结果解读](#结果解读)
5. [常见问题](#常见问题)
6. [最佳实践](#最佳实践)

---

## 🚀 快速开始

### 1. 上传固件

```bash
# Web 界面
1. 打开 http://localhost:8000
2. 点击"上传固件"按钮
3. 选择固件文件（.bin, .img, .hex 等）

# API 方式
curl -X POST "http://localhost:8000/api/upload" \
  -F "file=@firmware.bin" \
  -F "firmware_type=auto"
```

### 2. 开始扫描

```bash
# Web 界面
点击"开始分析"按钮，等待扫描完成

# API 方式
curl -X POST "http://localhost:8000/api/scan/{task_id}"
```

### 3. 查看结果

```bash
# Web 界面
- 仪表盘查看合规评分
- 下载审计报告包（ZIP）

# API 方式
curl -X GET "http://localhost:8000/api/result/{task_id}"
```

---

## 🔍 能力边界

### 支持的扫描目标

| 类型 | 支持状态 | 说明 |
|------|----------|------|
| **二进制固件** | ✅ 完全支持 | Binwalk 解包 → Syft SBOM → grype CVE 匹配 |
| **文件系统镜像** | ✅ 完全支持 | SquashFS/JFFS2/UBI 直接扫描 |
| **MCU 固件** | ⚠️ 有限支持 | 字符串提取 + 模式匹配，组件识别有限 |
| **源码树** | ❌ 不支持 | 建议交付标准 SBOM (CycloneDX/SPDX) |

### 已知限制

#### 1. 源码交付物

**限制说明**:
- 平台不解析 OpenWrt/Buildroot/Yocto 构建系统 Makefile
- 源码树扫描结果为 0 组件/0 CVE（技术路线固有限制）

**建议方案**:
- 要求供应商交付固件镜像或标准 SBOM（CycloneDX/SPDX）
- 或使用内部工具进行源码分析（非平台功能）

**技术原因**:
- Syft 不支持 buildroot 风格 Makefile 解析
- 自研解析器需覆盖 OpenWrt/Buildroot/Yocto 三大构建系统
- 维护成本高，且源码交付在汽车行业罕见

#### 2. 内核检测

**限制说明**:
- Linux Kernel CVE 检测覆盖率 ~50%
- 部分内核漏洞可能遗漏

**改进计划**:
- v3.0.0 计划增强内核检测能力

#### 3. 零日漏洞

**限制说明**:
- 仅检测已知 CVE（NVD/Grype DB）
- 不检测 0-day 漏洞

**建议**:
- 结合其他安全工具进行综合评估

---

## 📊 扫描目标类型

### 1. 二进制固件（推荐）

**适用场景**: 汽车 ECU 固件、路由器固件、IoT 设备固件

**支持格式**:
- `.bin` - 原始二进制固件
- `.img` - 磁盘镜像
- `.hex` - Intel HEX 格式
- `.srec` - Motorola S-Record 格式

**扫描流程**:
```
固件文件 → Binwalk 解包 → Syft SBOM → grype CVE 匹配 → R155 合规检查
```

**预期结果**:
- 组件识别：10-50 个（取决于固件复杂度）
- CVE 检测：基于 Grype DB（~200,000+ CVE）

### 2. 文件系统镜像

**适用场景**: SquashFS/JFFS2/UBI 文件系统

**支持格式**:
- `.squashfs` - SquashFS 镜像
- `.jffs2` - JFFS2 镜像
- `.ubi` - UBI 镜像

**扫描流程**:
```
文件系统镜像 → 直接扫描 → Syft SBOM → grype CVE 匹配
```

**预期结果**:
- 组件识别：5-30 个（取决于安装的软件包）
- CVE 检测：基于 Grype DB

### 3. MCU 固件（有限支持）

**适用场景**: 单片机固件、RTOS 固件

**支持格式**:
- `.bin` - 原始二进制
- `.hex` - Intel HEX
- `.elf` - ELF 可执行文件

**扫描流程**:
```
MCU 固件 → 字符串提取 → 模式匹配 → 组件识别（有限）
```

**预期结果**:
- 组件识别：0-5 个（仅识别常见 RTOS/库）
- CVE 检测：有限（仅匹配已知字符串模式）

**识别能力**:
- FreeRTOS, lwIP, wolfSSL, mbedTLS, OpenSSL
- uCLibc, BusyBox, Zlib, Newlib

### 4. 源码树（不支持）

**适用场景**: OpenWrt/Buildroot/Yocto 源码

**不支持原因**:
- Syft 不识别 buildroot 风格 Makefile
- 自研解析器需覆盖三大构建系统，维护成本高
- 汽车行业源码交付罕见

**建议方案**:
1. 要求供应商交付固件镜像
2. 或交付标准 SBOM（CycloneDX/SPDX）
3. 或使用内部源码分析工具（非平台功能）

---

## 📋 结果解读

### CVE 严重程度

| 等级 | CVSS 分数 | 说明 | 建议 |
|------|-----------|------|------|
| **Critical** | 9.0-10.0 | 严重漏洞，可远程代码执行 | 立即修复 |
| **High** | 7.0-8.9 | 高危漏洞，可导致数据泄露 | 优先修复 |
| **Medium** | 4.0-6.9 | 中危漏洞，需要特定条件 | 计划修复 |
| **Low** | 0.1-3.9 | 低危漏洞，影响有限 | 可选修复 |

### EPSS 评分

| 评分 | 说明 | 建议 |
|------|------|------|
| **0.7-1.0** | 极高概率被利用 | 立即处理 |
| **0.4-0.7** | 高概率被利用 | 优先处理 |
| **0.1-0.4** | 中等概率 | 计划处理 |
| **0.0-0.1** | 低概率 | 可选处理 |

### R155 合规评分

| 分数 | 等级 | 说明 |
|------|------|------|
| **90-100** | ✅ 优秀 | 完全符合 R155 要求 |
| **70-89** | ⚠️ 良好 | 基本符合，有小幅改进空间 |
| **50-69** | ❌ 不合格 | 存在严重合规风险 |
| **<50** | ❌ 严重不合格 | 需立即整改 |

---

## ❓ 常见问题

### Q1: 为什么源码树扫描结果为 0？

**A**: 平台当前不支持解析 OpenWrt/Buildroot/Yocto 构建系统 Makefile。这是技术路线的固有限制，不是缺陷。

**建议**:
- 要求供应商交付固件镜像或标准 SBOM
- 或使用内部工具进行源码分析

### Q2: 为什么组件识别数量比预期少？

**A**: 可能原因：
1. Syft 未识别某些库文件
2. 自研提取器仅覆盖常见模式
3. 固件经过Strip/混淆处理

**建议**:
- 查看详细日志，了解哪些组件被遗漏
- 考虑使用 Phase 4（构建配方 SBOM）进行补充

### Q3: CVE 匹配结果与 grype CLI 不一致？

**A**: v2.5.0 已使用 grype CLI 作为主匹配引擎，结果应与 grype CLI 基准一致（偏差 ≤20%）。

如果存在差异：
1. 检查 grype CLI 版本（要求 v0.115+）
2. 查看扫描日志，确认 grype CLI 是否成功调用
3. 如果降级到自研匹配器，结果可能不同

### Q4: 如何处理误报？

**A**: 
1. 检查 CVE 的版本约束（fixed_version 字段）
2. 确认组件版本是否真的受影响
3. 使用 R155 合规检查中的"豁免"功能

### Q5: 为什么某些字段为空（cvss_score, published_date）？

**A**: 
- v2.4.3-hotfix 已修复字段补全问题
- 如果仍为空，可能是：
  1. Grype DB 中该 CVE 信息不完整
  2. EPSS 数据未下载（首次使用需自动下载）

---

## 💡 最佳实践

### 1. 固件准备

- ✅ 使用原始固件文件（未经修改）
- ✅ 确认固件完整性（MD5/SHA256）
- ✅ 记录固件版本和供应商信息

### 2. 扫描策略

- ✅ 先扫描单个固件验证流程
- ✅ 再批量扫描多版本固件
- ✅ 定期更新 Grype DB（每周一次）

### 3. 结果分析

- ✅ 优先处理 Critical/High + EPSS > 0.7 的 CVE
- ✅ 关注 R155 不合规项（CVSS >= 7.0 且 >180 天未修复）
- ✅ 导出审计报告包用于合规申报

### 4. 合规申报

- ✅ 使用"审计报告包"功能生成完整文档
- ✅ 保存基线数据用于下次对比
- ✅ 记录整改措施和修复时间

---

## 📞 技术支持

- **Email**: zhu80k@163.com
- **GitHub Issues**: https://github.com/Jackson8ok/afvs-auto-firmware-vulnerability-scanner/issues
- **文档**: [README.md](./README.md) | [DEPLOYMENT.md](./DEPLOYMENT.md)

---

**版本**: v2.5.0  
**最后更新**: 2026-08-19
