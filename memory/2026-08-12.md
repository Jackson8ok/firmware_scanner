# 2026-08-12 玄武 v2.4.1-hotfix 严格自评审

## 实施内容

### 修复 (P0)
1. **time.sleep 阻塞事件循环** - `scanner/task_queue.py`: `wait_for_completion` 改为 `async def` + `await asyncio.sleep`，更新所有调用方
2. **CORS 全放开** - `api/main.py` + `config.yaml`: 将 `cors_allowed_origins="*"` 改为从 config 读取允许列表
3. **上传无大小限制** - `api/main.py`: 添加 100MB 文件大小限制，超出返回 413

### 清理 (P1)
4. **main_backup.py 重复代码** - 移至 `.trash/`，确认无引用

### 评审报告
- `docs/STRICT_REVIEW_v2.4.1.md` - 详细报告 + 修复状态追踪
- `docs/STRICT_REVIEW_v2.4.1.py` - 可复用的扫描脚本

## 结论
3 个 HIGH 全部修复 ✅，8 个 MEDIUM 已记录为 v2.5.0 后续任务。
