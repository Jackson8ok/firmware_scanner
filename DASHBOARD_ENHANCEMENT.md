# 📊 Dashboard 增强版 - 功能文档（v2.2）

## ✨ 新增功能概览

### v2.2 - 数据可视化与高级筛选

本次更新实现了完整的 Dashboard 增强功能，包括丰富的图表展示、智能筛选和多种报告导出方式。

---

## 🎨 核心功能

### 1. 四图联动可视化系统

#### (1) 严重程度分布饼图
- **位置**: 仪表板右上角
- **数据**: critical/high/medium/low CVE 占比
- **交互**: 显示百分比标签和图例
- **技术**: Canvas 2D 原生绘制

#### (2) Top 10 高优先级柱状图
- **位置**: 仪表板右下角
- **数据**: 按优先级分数排序的前 10 个 CVE
- **交互**: 渐变色填充，数值悬停显示
- **特点**: 突出最关键的安全风险

#### (3) 近 7 天扫描趋势折线图
- **位置**: 仪表板下方左侧
- **数据**: 每日 CVE 总数 + 严重漏洞数两条线
- **交互**: 数据点圆点标记，平滑曲线
- **价值**: 追踪安全状况变化趋势

#### (4) R155 合规评分雷达图
- **位置**: 仪表板下方右侧
- **6 个维度**:
  1. 软件供应链安全
  2. 漏洞管理
  3. 产品安全
  4. 认证机制
  5. 加密保护
  6. 日志审计
- **颜色编码**: 蓝色半透明填充，直观展示强弱项

---

### 2. 智能筛选控制面板

**筛选条件**:
- **时间范围**: 7 天 / 30 天 / 90 天 / 全部
- **严重程度**: 全部 / 严重 / 高危 / 中危 / 低危
- **固件类型**: 全部 / Binary / HEX / SREC / SquashFS

**操作按钮**:
- **应用筛选**: 立即刷新图表和数据表
- **重置**: 恢复默认状态

**技术实现**:
```javascript
currentFilters = {
    timeRange: '7',      // 时间范围
    severity: 'all',     // 严重程度
    type: 'all'          // 固件类型
}
```

---

### 3. 多格式报告导出

#### Excel 导出 (.xlsx)
- **功能**: 完整漏洞详情表格
- **样式**: 
  - 彩色表头
  - 严重程度颜色编码
  - 自适应列宽
- **字段**: CVE ID, 组件，版本，严重程度，CVSS, 优先级，描述，R155 合规，固定版本

#### PDF 导出
- **方式**: 浏览器打印对话框
- **内容**: 
  - 统计摘要卡片
  - 漏洞列表表格
  - 品牌化页眉
- **优势**: 可直接用于合规报告

#### YAML 导出
- **格式**: 结构化文本
- **用途**: 后续处理、CI/CD 集成
- **包含**: 所有原始数据

---

### 4. 扫描历史跟踪表

**列定义**:
| 列名 | 说明 |
|-----|------|
| 日期 | 扫描完成时间 |
| 固件 ID | 短标识符 (前 8 位) |
| 文件名 | 原始文件名 |
| 类型 | bin/hex/srec/squashfs |
| 总 CVE | 漏洞总数 |
| 严重 | 严重漏洞数 (红色标注) |
| 高危 | 高危漏洞数 (橙色标注) |
| R155 不合规 | 不合规项数量 |
| 操作 | 查看/YAML 下载按钮 |

**特性**:
- 自动分页（每页 20 条）
- 实时更新（每 10 秒刷新）
- 状态颜色编码

---

## 🖥️ 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│                      [头部 Logo 和标题]                        │
├─────────────────────────────────────────────────────────────┤
│                     [上传和批量扫描区]                       │
├─────────────────────────────────────────────────────────────┤
│                    [队列状态监控面板]                        │
├─────────────────────────────────────────────────────────────┤
│                  [🔍 智能筛选控制面板]                       │
├─────────────────────────────────────────────────────────────┤
│                   [统计数字卡片 (6 个)]                       │
├───────────────────────────────┬─────────────────────────────┤
│   [饼图：严重程度分布]        │  [柱状图：Top10 高优先]       │
├───────────────────────────────┼─────────────────────────────┤
│   [折线图：扫描趋势]          │  [雷达图：R155 合规]          │
├─────────────────────────────────────────────────────────────┤
│                  [漏洞详情表格 + 筛选器]                     │
├─────────────────────────────────────────────────────────────┤
│                     [扫描历史记录表]                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 使用示例

### Web 界面操作流程

1. **首次访问 Dashboard**
   - 自动加载最新的扫描结果
   - 显示最近 7 天的扫描趋势
   - 渲染所有 4 张图表

2. **应用筛选**
   ```
   步骤：
   1. 选择"时间范围：30 天"
   2. 选择"严重程度：高危"
   3. 点击"应用筛选"
   
   效果：
   - 饼图只显示高危漏洞分布
   - 柱状图更新为高危 TOP10
   - 表格过滤出高危漏洞
   ```

3. **导出报告**
   ```
   方法 1: Excel
   - 点击"📊 Excel 导出"
   - 自动下载 .xlsx 文件
   
   方法 2: PDF  
   - 点击"📄 PDF 导出"
   - 打开新窗口 → 点击打印 → 选择"另存为 PDF"
   
   方法 3: YAML
   - 从历史记录点击"YAML"按钮
   - 下载结构化数据
   ```

4. **查看历史扫描**
   ```
   步骤：
   1. 滚动到页面底部
   2. 找到感兴趣的扫描记录
   3. 点击"查看"按钮切换到该结果
   ```

---

## 🔧 API 端点清单

| 端点 | 方法 | 功能 |
|-----|------|-----|
| `/api/tasks?status=completed` | GET | 获取已完成任务列表 |
| `/api/report/excel` | POST | 生成 Excel 报告 |
| `/api/reports/{task_id}` | GET | 下载 YAML 报告 |
| `/api/task/{task_id}` | GET | 获取单个任务详情 |

---

## 💻 技术架构

### 前端组件结构

```
frontend/
├── templates/
│   └── index.html              # 主页面模板
├── static/
│   ├── styles.css              # 全局样式
│   ├── app.js                  # 主应用逻辑 (v2.2)
│   └── charts.js               # 图表组件库 (新增)
```

### 图表组件设计模式

```javascript
// 统一接口设计
class BaseChart {
    constructor(canvasId) { /* ... */ }
    render(data) { /* 抽象方法 */ }
    drawTitle(text, x, y) { /* 通用标题 */ }
    drawNoDataText(...) { /* 空状态处理 */ }
}

// 具体实现
class SeverityPieChart extends BaseChart { /* ... */ }
class PriorityBarChart extends BaseChart { /* ... */ }
class TrendLineChart extends BaseChart { /* ... */ }
class ComplianceRadarChart extends BaseChart { /* ... */ }
```

### 纯原生 JS 实现

- ✅ 无外部依赖（不使用 Chart.js/ECharts 等）
- ✅ 仅使用 Canvas 2D API
- ✅ 代码体积更小（~18KB 压缩后）
- ✅ 完全可控的样式和行为

---

## 📊 性能优化

### 1. 增量更新
- 图表只在数据变化时重绘
- 筛选操作不会触发全量刷新

### 2. 懒加载
- 扫描历史只加载最新 20 条
- 更多数据按需分页

### 3. 缓存策略
```javascript
// 本地缓存扫描结果
if (!scanData || scanData.firmware_id !== currentScanId) {
    await loadScanResult(currentScanId);
}
```

### 4. 自动刷新
- 每 10 秒轮询一次队列状态和历史
- 用户操作后立即刷新，避免等待

---

## 🎯 UX 细节

### 动画效果
```css
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

.stat-card, .chart-card {
    animation: fadeIn 0.5s ease-out;
}
```

### 通知提示
- ✅ 成功：绿色，右上方滑入
- ⚠️ 警告：黄色，停留 5 秒
- ❌ 失败：红色，手动关闭

### 响应式断点
- **> 1024px**: 4 列网格，完整功能
- **768-1024px**: 2 列网格，部分折叠
- **< 768px**: 单列布局，垂直堆叠

---

## 🧪 测试场景

### 基础功能测试
1. **图表渲染**
   - [ ] 有数据时正常显示
   - [ ] 无数据时显示占位符
   - [ ] 切换数据时正确更新

2. **筛选功能**
   - [ ] 各筛选条件独立工作
   - [ ] 组合筛选准确
   - [ ] 重置功能正常

3. **导出功能**
   - [ ] Excel 文件格式正确
   - [ ] PDF 可打印
   - [ ] YAML 语法无误

4. **历史记录**
   - [ ] 列表正确显示
   - [ ] 查看详情跳转正常
   - [ ] 分页加载可用

### 边界情况测试
- 1000+ 漏洞数据时的性能
- 网络中断后的重试机制
- 并发多个扫描任务的 UI 更新

---

## 🚀 部署建议

### 生产环境启用

```bash
# 1. 确保所有静态文件已压缩
cd /mnt/workspace/firmware_scanner
find frontend/static -name "*.js" -o -name "*.css" | xargs gzip -9

# 2. 配置 Nginx 缓存
location /static/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}

# 3. 启用 Gzip 压缩
gzip on;
gzip_types text/css application/javascript;
```

---

## 📈 下一步计划

根据路线图，接下来应该继续：

⏳ **W1-D4: R155 合规检查深化**
   - 欧盟法规知识库建设
   - 自动化合规报告模板
   - 合规得分计算算法优化

或

⏳ **W2-D1: CI/CD流水线集成**
   - GitHub Actions 工作流
   - 自动触发扫描
   - 质量门禁配置

---

## 📚 参考资料

- [R155 法规原文](https://UNECE.org/transmission/r155)
- [OWASP Mobile Security Guidelines](https://owasp.org/www-project-mobile-security/)
- [NIST Cybersecurity Framework](https://www.nist.gov/cyberframework)

---

**作者**: 玄武 Team  
**版本**: 2.2-alpha  
**最后更新**: 2026-07-22  
**许可证**: MIT
